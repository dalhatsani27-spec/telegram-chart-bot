name: Verify trendline v2

on:
  push:
    branches:
      - trendline-20sma-map-v2

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Compile Python modules
        run: python -m py_compile trendline_mapper_v2.py strategies.py sitecustomize.py